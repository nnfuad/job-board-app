# ✅ docker-compose.yml - BETTER HEALTHCHECKS & CORS
backend:
  environment:
    FRONTEND_URL: http://localhost  # ← FIXED (was job_board_web)
  healthcheck:
    test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://localhost:8080/health']